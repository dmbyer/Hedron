namespace Hedron.Core
{
    /// <summary>Named equipment slots a character can fill.</summary>
    public enum WornSlot
    {
        MainHand,
        OffHand,
        Head,
        Chest,
        Feet,
    }
}
