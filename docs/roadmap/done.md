# Done

> Short ledger of completed roadmap items. One line per item; full detail in [`completed/`](completed/). For what's next, see [`plan.md`](plan.md).

| Phase / slice | Outcome | Detail |
|---|---|---|
| **Phase 1 — Strip** | Demolished legacy code; bumped to `net8.0`; scrapped Blazor; removed `Data` and `Bot` projects; kept ECS primitives only where they matched the idealized API. | [`completed/phase-1-strip.md`](completed/phase-1-strip.md) |
| **Phase 1.5 — Ticket A (ECS redesign)** | One-world model, `Entity(uint Id)` wrapper, `TemplateRegistry`, archetypes restricted to validation + detection, `[Persistent]` per-component, effects split persistent/transient. | Folded into [`completed/phase-1-strip.md`](completed/phase-1-strip.md) |
| **Phase 2 — Foundation / MVP** | Built target architecture from scratch; shipped multi-user telnet walking simulator with `look`, six-direction movement, and `say`. Acceptance test passed. | [`completed/phase-2-mvp.md`](completed/phase-2-mvp.md) |
| **Phase 3 slice 1 — Persistence substrate** | `PersistenceSystem` with `[Persistent]`-tagged components, `System.Text.Json` serializer, dirty-tracking, atomic writes, silent hydration with `EntityHydratedEvent` / `WorldLoadedEvent`. No gameplay change; unlocks every subsequent slice. | [`completed/slice-1-persistence-substrate.md`](completed/slice-1-persistence-substrate.md) · spec: [`implementation-plans/persistence-substrate.md`](../architecture/06-persistence.md) |
| **Phase 3 slice 2 — World content loading + admin substrate** | YAML content pipeline (`TemplateRegistry` + per-module `ITemplateDeserializer`); `WorldContentLoader` with empty-dir void fallback; admin substrate (`IAdminAuthorizer` + `@spawn`/`@teleport`/`@dig`/`@reload`); `AdminAuditHandler`; hardcoded `WorldBootstrap` removed. Resolves Ticket B. | [`completed/slice-2-world-content-and-admin-substrate.md`](completed/slice-2-world-content-and-admin-substrate.md) · spec: [`implementation-plans/world-content-loading-and-admin-substrate.md`](../features/world/world.md) |
| **Phase 3 slice 3 — Command framework** | `CommandContext`, declarative `ICommandArgumentParser` + `CommandArgumentSchema`, structural privilege gate (`IAuthorizationRequirement`/`IAuthorizationChecker`/`AdminRequirement`), `CommandExecutedEvent` + `CommandLoggingHandler`, `help`/`commands` commands + `HelpModule`, minimal output seam (`IOutputMessage`, `PlainMessage`, `HelpIndexMessage`, `HelpEntryMessage`, stringify-and-forward `IOutputWriter`). All 12 existing commands refactored onto the new shape. Added Initiators tier to architecture. | [`completed/slice-3-command-framework.md`](completed/slice-3-command-framework.md) · spec: [`implementation-plans/command-framework.md`](../features/commands/commands.md) |
| **Phase 3 slice 3a — Command prefix matching** | Dynamic prefix resolution in `CommandDispatcher` (`lo`→`look`); `CommandMatchingMode` (`Partial`/`Full`) per command; `IVerbRegistry` read-only seam over the command namespace; `IArgumentResolver` interface + parser call-site wiring (concrete impls deferred to slice 6); alias surfacing in `help`/`commands` output. | [`completed/slice-3a-command-prefix-matching.md`](completed/slice-3a-command-prefix-matching.md) · spec: [`implementation-plans/command-prefix-matching.md`](../features/commands/commands.md) |
| **Phase 3 slice 4 — Output framework** | `IOutputFormatter`/`IOutputFormatterRegistry`/`TelnetOutputFormatter` (ANSI palette + `<role>text</role>` inline markers); formatter-backed `OutputWriter` replacing slice-3 stringify-and-forward; `SupportsColor`/`TransportKey` on `ISession`; `RoomDescriptionMessage` + `MovementMessage` shapes; `IBroadcastSystem` extended with typed `SendToRoomAsync` (audience-filter predicate), `SendToAllAsync`, and recomposed `SendRoomDescriptionAsync`; `Output:DefaultColor` config key. | [`completed/slice-4-output-framework.md`](completed/slice-4-output-framework.md) · spec: [`implementation-plans/output-framework.md`](../features/output/output.md) |
| **Phase 3 slice 5 — Account / character creation** | Full login/registration flow (`LoginFlow` Initiator) replacing throwaway names; `AccountComponent` + `CharacterComponent` (both `[Persistent]`); `IAccountSystem` with PBKDF2-SHA256 `IPasswordHasher`; `CharacterHydrationHandler` validating character locations on `WorldContentReadyEvent`; `whois` admin command; `LocationComponent` promoted to `[Persistent]`; `PlayerSessionHandler` rewritten to be entity-lifecycle-free. | [`completed/slice-5-account-character-creation.md`](completed/slice-5-account-character-creation.md) · spec: [`implementation-plans/account-character-creation.md`](../features/accounts/accounts.md) |
| **Phase 3 slice 5a — Bare-bones content spawning** | `dig <direction> [name]` replaces the slice-2 connect-to-existing syntax; new `set <property> <value>` command; `IRoomBuilderSystem` domain system (CreateRoom, LinkExits, SetRoomName, SetRoomDescription); `RoomComponent` tagged `[Persistent]` (closes slice-2 gap); two new admin events; `AdminAuditHandler` and `PersistenceHandler` extended; `add-command` skill updated for INV-8 clarity. | [`completed/slice-5a-bare-bones-content-spawning.md`](completed/slice-5a-bare-bones-content-spawning.md) · spec: [`implementation-plans/bare-bones-content-spawning.md`](../features/world/world.md) |
| **Phase 3 slice 5b — Persistence two-level model** | `PersistentEntity` zero-data marker component opts entities into persistence (level 1); `[Persistent]` on component types controls snapshot inclusion (level 2); area-scoped periodic flush replaces global dirty-set sweep; `PersistenceHandler` deleted; save-on-change (`SaveEntityAsync`) wired directly into admin commands and lifecycle handlers. | [`completed/slice-5b-persistence-two-level-model.md`](completed/slice-5b-persistence-two-level-model.md) · spec: [`implementation-plans/persistence-two-level-model.md`](../architecture/06-persistence.md) |
| **Phase 3 slice 6 — Items + inventory + `get`/`drop`/`look <item>`** | `ItemDataComponent` + `InventoryComponent` (`[Persistent]`); `ItemType` enum; admin `mkitem`/`setitem`; YAML `kind: item` content pipeline; `get`/`drop`/`inventory` player commands with `ItemInRoomResolver`/`ItemInInventoryResolver`; `IArgumentResolver` return type updated to `IReadOnlyList<ResolvedCandidate>?` with keyword-deduplication; `ItemInteractionHandler` broadcast fan-out; `look <item>` with room + inventory fallback; flows 9–12 added to the canonical flows catalog. | [`completed/slice-6-items-and-inventory.md`](completed/slice-6-items-and-inventory.md) |
| **Phase 3 slice 7 — Equipment + `wear`/`remove`** | `WornSlot` enum + `EquipmentComponent` (`[Persistent]`, cross-cutting); `WornSlots` field on `ItemDataComponent`; `IEquipmentSystem`/`EquipmentSystem` (slot query, equip, remove, implicit-swap); `wear`/`remove`/`equipment` player commands; `ItemInEquipmentResolver`; `EquipmentInteractionHandler` broadcast fan-out; `setitem slot` admin extension; YAML `wornSlots` field; `EquipmentDisplayMessage` + formatter case; hydration guard; flows 13–14 added. | [`completed/slice-7-equipment.md`](completed/slice-7-equipment.md) |
| **Phase 3 slice 8 — Mobs (basic entity model and spawn)** | `MobDataComponent` + `MobType` enum (cross-cutting); `MobTemplate` + `MobTemplateDeserializer` (`kind: mob` YAML pipeline); `IMobBuilderSystem`/`MobBuilderSystem`; `IMobContentWriter`/`MobContentWriter`; admin `mkmob`/`setmob` commands; `WorldContentLoader` extended with mob loading and `PlaceMobsInRooms`; `RoomDescriptionMessage.Mobs` field; `TelnetOutputFormatter` mob rendering; `AdminAuditHandler` extended for mob events; `MobsModule` DI entry point. | [`completed/slice-8-mobs.md`](completed/slice-8-mobs.md) · spec: [`implementation-plans/mobs.md`](../features/mobs/mobs.md) |
| **Phase 3 slice 8a — Attributes and vitals** | `AttributesComponent` (Level, Str, Dex, Con) + `PoolsComponent` (MaxHp, CurrentHp) as cross-cutting `[Persistent]` components; `IAttributeSystem`/`AttributeSystem`; `score` player command; `setplayer` admin command; `MobTemplate` extended with attribute fields; `IMobBuilderSystem.SetAttribute`; `setmob` extended for `level`/`hp`/`str`/`dex`/`con`; `CharacterHydrationHandler` + `AccountSystem.CreateCharacterAsync` extended; `PlayerAttributeSetByAdminEvent`; `ScoreDisplayMessage` + formatter; `AttributesModule` DI entry point. | [`completed/slice-8a-attributes-and-vitals.md`](completed/slice-8a-attributes-and-vitals.md) · spec: [`implementation-plans/attributes.md`](../features/character-stats/character-stats.md) |
| **Phase 3 slice 9-a — Entity state management** | `EntityStateFlags` `[Flags]` enum + `EntityStateComponent` (transient, not `[Persistent]`); `IEntityStateService`/`EntityStateService` with static transition-rule table enforcing `InCombat`/`Resting`/`Incapacitated` guard rules; `EntityStateChangedEvent` published by callers; `EntityStateModule` DI entry point; enum renamed `EntityStateFlags` (type-rename convention) to resolve the C# namespace/type collision with the `EntityState` module. | [`completed/slice-9a-entity-state-management.md`](completed/slice-9a-entity-state-management.md) · spec: [`implementation-plans/entity-state-management.md`](../features/combat/combat.md) |
| **Phase 3 slice 9-b — Time system (heartbeat)** | `HeartbeatBackgroundService` (Initiator; `PeriodicTimer` at `Heartbeat:IntervalMs`, default 2000 ms) publishes `HeartbeatTickEvent { TickId, Timestamp, Elapsed }` on every tick; `TimeModule` DI anchor; Flow 16 added to the canonical flows catalog; Flow 1 (server startup) updated to include the new last hosted service. | [`completed/slice-9b-time-system.md`](completed/slice-9b-time-system.md) · spec: [`implementation-plans/time-system.md`](../features/world/world.md) |
| **Phase 3 slice 9-c — Stat computation system** | `IStatSystem`/`StatSystem` aggregates effective attack power, defense, and HP from base attributes + equipment bonuses (no `is`/`as`, no list allocation); `IAttributeSystem.SetCurrentHp` write seam with `[0, MaxHp]` clamp; `ItemDataComponent.DamageBonus` field + YAML/template/writer round-trip; `setitem dmg <n>` admin command extension; `StatsModule` DI entry point. | [`completed/slice-9c-stat-system.md`](completed/slice-9c-stat-system.md) · spec: [`implementation-plans/stat-system.md`](../features/character-stats/character-stats.md) |
| **Phase 3 slice 9 — Combat** | Core melee loop: `kill <mob>` / `flee` commands; `CombatStateComponent`; `ICombatSystem`/`CombatSystem` (target lookup, StartCombat/EndCombat, round resolution via `IStatSystem`); `CombatTickHandler` (heartbeat subscriber, deduplication, terminal outcomes); `CombatHandler` (output fan-out); `CombatMobDeathHandler` (mob death, blueprint slot free, entity destroy); `CombatStartedEvent`/`CombatRoundEvent`/`CombatEndedEvent`; `CombatModule` DI entry point; player incapacitation stub (HP clamp to 1) for slice 10. | [`completed/slice-9-combat.md`](completed/slice-9-combat.md) · spec: [`implementation-plans/combat.md`](../features/combat/combat.md) |
| **Phase 3 slice 9-d — Stat & resource substrate** | Replaced the interim `Str`/`Dex`/`Con` stub with the four gameplay-model attributes (Mind/Body/Spirit/Attunement) + Mana/Stamina/Astra pools; added the `ScoreId` vocabulary, `IStatRegistry`, and the generalized `IStatSystem.Get(ScoreId)` read seam; `CharacterDefaults:` config; combat reads Body. The substrate every later spine writes to. | [`completed/slice-9d-stat-resource-substrate.md`](completed/slice-9d-stat-resource-substrate.md) · spec: [`implementation-plans/stat-resource-substrate.md`](../features/character-stats/character-stats.md) |
| **Phase 3 slice 9-e — Effect substrate** | The unified effect model (`Effect` kinds/lifetime/stacking/phase/Power) + core `IEffectSystem` (apply/stack/order/tick/persist); single lifetime-filtered `EffectsComponent`; `StatSystem.Get` folds `StatModifier`s transparently; heartbeat `EffectTickHandler`; admin `affect`/`affects`. The bedrock skills/potions/curses build on. | [`completed/slice-9e-effect-substrate.md`](completed/slice-9e-effect-substrate.md) · spec: [`implementation-plans/effect-substrate.md`](../features/effects/effects.md) |
| **Phase 3 slice 10 — Death and respawn** | Player incapacitation → bleed-out → death → respawn (HP-threshold-driven via the single `IDeathSystem.OnHpChanged` seam); dispatcher incapacitation gate (`UsableWhileIncapacitated`); `SetCurrentHp` floor lowered to `Death:HpFloor`; `RemoveImpermanent` effect expiry; persisted `RespawnComponent` + `setrespawn`; `MobDiedEvent` killer seam; INV-22 boundary-save categories. | [`completed/slice-10-death-and-respawn.md`](completed/slice-10-death-and-respawn.md) · spec: [`implementation-plans/death-and-respawn.md`](../features/combat/combat.md) |
| **Phase 3 slice 11-a — Ability substrate** | Unified skill/spell model (`AbilityDefinition`/`IAbilitySystem`/`AbilitiesComponent`); multi-pool costs; transient cooldowns ticked on heartbeat; passive `WhileKnown` effects derived via new `IEffectContributor` seam (INV-24); `AbilityRegistry`; admin `teach`/`useability`; player `abilities`/`skills`/`spells`. | [`completed/slice-11a-ability-substrate.md`](completed/slice-11a-ability-substrate.md) · spec: [`implementation-plans/ability-substrate.md`](../features/abilities/abilities.md) |
| **Phase 3 slice 11-b — Ability invocation & combat targeting** | Dynamic per-actor skill verb resolution (third dispatcher phase via `IAbilityVerbResolver`); `cast <spell>` command; `AbilityInvocationPipeline`; offensive abilities open combat and resolve via `ICombatSystem.ResolveAbilityStrike`; starting abilities granted at character creation from config; `MobInRoomResolver` extracted (INV-19 3rd-consumer threshold). | [`completed/slice-11b-ability-invocation.md`](completed/slice-11b-ability-invocation.md) · spec: [`implementation-plans/ability-invocation.md`](../features/abilities/abilities.md) |
| **Phase 3 slice 11-c — Resource regeneration + `rest`** | Out-of-combat baseline regen (HP/Mana/Stamina/Astra) via `RegenerationSystem` + `RegenerationTickHandler`; `rest`/`stand` commands; movement breaks rest; `EntityStateService` auto-exit table (`InCombat → Resting`) centralises the mutual-exclusion invariant; effects-based regen (`regen` spell) stacks additively. | [`completed/slice-11c-resource-regeneration.md`](completed/slice-11c-resource-regeneration.md) · spec: [`implementation-plans/resource-regeneration.md`](../features/character-stats/character-stats.md) |
| **Phase 3 — Output batching + player prompt** | Session-scoped `ISessionOutputBuffer`/`SessionBufferRegistry`; `SessionBufferedOutputWriter` replacing per-request `OutputWriter`; `CategoryFlushPolicy` (Chat=Immediate, rest=Batched); `IPromptSource` core port + `PromptComposerSystem` (domain); `OutputFlushTickHandler` (p=85); `PromptMessage`/`PoolDisplay` typed shapes; dispatcher `try/finally` flush; `PlainMessage` constructor now requires explicit `OutputCategory`. | [`completed/output-batching.md`](completed/output-batching.md) · spec: [`implementation-plans/prompt-and-output-batching.md`](../features/output/output.md) |
| **Phase 3 slice 11-d — Aspect & Registry Foundation** | Generic `IRegistry<TKey,TDef>` + `DefinitionRegistry` base (Ability/Effect/Stat retrofit); `AspectId`/`AspectRegistry`/`IAspectSystem` (aspect-typed combat damage, affinity boost, independent per-aspect resistance); `AspectAffinitiesComponent` (`[Persistent]`); startup fail-fast `RegistryValidationBootstrap`; generic `defs` admin inspector; flows 01/18/24/26 updated. | [`completed/aspect-foundation.md`](completed/aspect-foundation.md) · spec: [`implementation-plans/aspect-foundation.md`](../features/aspects/aspects.md) |
| **Phase 4 — Testing harness & backfill** | Stood up `Hedron.Tests` xUnit project + shared harness (`RecordingEventBus`, `EntityBuilder`, `FakeRandom`, `FakeClock`, `RecordingOutput`, `PersistenceTestHarness`, `Ticks`); architecture-guard suite enforcing INV-3/5/13/23/26 + DI-smoke; `IClock`/`SystemClock` seam + `SpawnSystem`/`AccountSystem` refactored off wall-clock; `CompositionRoot` extracted; 566 tests across Wave 1 (Combat/Effects/Stats/Abilities/Death/Persistence/Registry) + Wave 2 (all 11 remaining systems) backfill; CI workflow on every PR; `ItemSystem` INV-21 bug fixed. | [`completed/testing-harness-and-backfill.md`](completed/testing-harness-and-backfill.md) · spec: [`implementation-plans/testing-harness-and-backfill.md`](../architecture/07-testing.md) |
| **`IOptions<T>` sweep — typed config options** | Created `WorldOptions` + `PersistenceOptions`; wired `Configure<T>()` for all three previously-unfinished sections (`World:`, `Persistence:`, `CharacterDefaults:`); replaced all raw `IConfiguration[...]` reads in `WorldContentLoader`, `RoomContentWriter`, `ItemContentWriter`, `MobContentWriter`, `PersistenceSystem`, `PersistenceFlushTimer`, and `AccountSystem`; updated `PersistenceTestHarness` and `AccountSystemTests`; env-var override (`HEDRON_World__ContentDirectory`, `HEDRON_Persistence__DatabasePath`) documented in backlog for machine-local world data. `AttributeSystem` cross-module dependency on `DeathOptions` tracked but not yet resolved. | |
| **Phase 3 — Area model + room–area membership** | Bidirectional area linking via new `IAreaSystem`/`AreaSystem` (`GetRoomsInArea`, `GetAreaForRoom`, `AssignRoomToArea`); `RoomComponent.AreaEntityId` resolved at startup by `WorldContentLoader.LinkRoomAreas`; `AreaTemplate` + `AreaTemplateDeserializer` extended with optional `AspectAffinities`; `RegistryValidationBootstrap` extended to validate area aspect compositions; `area`/`setarea` admin commands; `@dig` inherits source-room area; 34 new tests (587 total). | [`completed/area-model.md`](completed/area-model.md) · spec: [`implementation-plans/area-model.md`](../features/world/world.md) |
| **Phase 3 — Content-tooling platform** | UI-/headless-first content authoring. Shared content-definition layer (`IContentDefinitionCatalog`) + callable `IContentValidator` factored from `RegistryValidationBootstrap`; split hosted-service registration (`AddGameplayHostedServices`/`AddContentBootstrapHostedServices`); headless `generate` run-mode (`IContentGenerationSystem` + seeded `IRandom` → reproducible YAML worlds); in-process Blazor Server authoring editor (`Hedron.Web`) over area/room/item/mob with apply-via-reload; 619 tests total. | [`completed/content-tooling-platform.md`](completed/content-tooling-platform.md) · spec: [`implementation-plans/content-tooling-platform.md`](../features/admin-authoring/admin-authoring.md) |
| **Phase 3 — Wearable equipment expansion** | Worn gear now drives effective stats as data: `EquipmentStatBonus(ScoreId, int)` rows on `ItemDataComponent` + `EquipmentEffectContributor` (the INV-24 contributor seam — derived on read, two-slot dedupe) folded by `IStatSystem.Get(AttackPower\|Defense)`; flat `ItemDataComponent.DamageBonus` removed and migrated onto that path with combat repointed to `Get(...)`; `WornSlot` +9 for a full armor suit (doubled rings/wrists); `setitem bonus`/`clearbonus`, YAML `statBonuses:`, generator + Blazor row editor; 627 tests total. | [`completed/wearable-equipment-expansion.md`](completed/wearable-equipment-expansion.md) · feature: [`features/items/items.md`](../features/items/items.md) |
| **Phase 3 — Content-editor area filters, referential integrity & delete** | Offline Blazor editor gained area filters, lookup selection fields, and a dark-readable theme; catalog area-association read-model (`ContentSummary.AreaBlueprintId` + `RoomsInArea`); declared-edge `IContentReferenceIndex` (5 edges) backing `DeleteAsync` cascade-clear (file-only, INV-22/23), warn-but-allow `SaveAsync` (`ContentWriteResult.Warnings`), an Integrity broken-link sweep page, and bidirectional exit linking (`Direction.Opposite`, warn-and-skip); 687 tests total. | [`completed/content-editor-integrity.md`](completed/content-editor-integrity.md) · feature: [`features/admin-authoring/content-authoring.md`](../features/admin-authoring/content-authoring.md) |
| **Phase 3 — Currency foundation** | Entity-keyed `WalletComponent` ledger + pure `IWalletSystem` (Deposit/TryWithdraw/CanAfford/atomic `Transfer`/SetBalance); `CurrencyRegistry` (Spine F) with data-driven denomination ladders (Coin: `100c=10s=1g`); opt-in mob `CurrencyLootComponent` (uniform `IRandom` roll) auto-awarded to the killer on `MobDiedEvent` with ladder-formatted narration; `score` balances + `setwallet` admin grant (boundary save + audit); `Transfer` exposed for downstream economy verbs; enum-name wallet persistence; 776 tests total. | [`completed/currency-foundation.md`](completed/currency-foundation.md) · feature: [`features/economy/economy.md`](../features/economy/economy.md) |
| **Phase 3 slice 12b — Mob protection** | Cross-cutting `ProtectionComponent` (`[Flags] ProtectionFlags { None, Untargetable, EffectImmune }`, non-`[Persistent]` world content); attack gate via the shared `ICombatSystem.CanBeAttacked` query (consumed by `KillCommand` + `AbilityInvocationPipeline`, refusing before any state change/`CombatStartedEvent`); effect gate via a new structured `EffectApplyResult` on `IEffectSystem.Apply` (rejects beneficial **and** harmful effects when `EffectImmune`, no `EffectAppliedEvent`); authored by `IMobBuilderSystem.SetMobProtection` dual-write + `setmob protection` + `MobEditor` + YAML round-trip; the cross-cutting primitive Shopping (12c) consumes; 828 tests total. | [`completed/mob-protection.md`](completed/mob-protection.md) · feature: [`features/mobs/mobs.md`](../features/mobs/mobs.md#protection-invulnerability--immunity) |
| **Phase 3 slice 12c — Shopping** | Players `buy`/`sell`/`list` against shopkeeper mobs (the trade half of the economy feature; first `IWalletSystem.Transfer` consumer). `ShopComponent` (accepted currency, till seed, authored base-stock rows) + per-item `ShopStockComponent { Base\|Acquired, ExpiresAt? }` on the shopkeeper's real `InventoryComponent`; `IShopSystem` prices compute-on-read from `Value × ShopOptions` ratio and composes `IWalletSystem`+`IItemSystem`+`IClock`; `ItemBought/SoldEvent` drive the persistence-pool transition via `ItemContextHandler` (keep `BlueprintComponent`, INV-21); heartbeat restock + buy-back-expiry sweeps (deterministic, INV-26); new shared `IItemSystem.MoveBetweenInventories` seam; `MobInRoomResolver` relocated to `Mobs/Resolvers/`; authored via `SetMobShop`/`setmob shop`/Blazor/YAML. Shipped alongside: world-loader persisted-instance exclusion (INV-21 picked-up items no longer shadow world re-spawn) and `reload` reconciled to a full world rebuild (resets runtime instance state without dropping players); admin `list` renamed `listents`. 913 tests total. | [`completed/shopping.md`](completed/shopping.md) · feature: [`features/economy/economy.md`](../features/economy/economy.md) |
| **Phase 3 slice 12a — Item value** | Every item carries an intrinsic `ItemDataComponent.Value` (non-negative base-unit Coin `long`, default 0 = valueless/non-saleable; `[Persistent]`); `ItemTemplate.Value` + `Apply` copy + YAML round-trip; `IItemBuilderSystem.SetItemValue` dual-writes live entity + in-memory template (mirrors `SetItemType`); `setitem <bp> value <n>` admin verb (edge-rejects negatives/non-integers, rides existing publish + YAML-persist path); Blazor `ItemEditor` numeric Value row. The derive-only price substrate slice 12c consumes; no consumer wired here; 801 tests total. | [`completed/item-value.md`](completed/item-value.md) · feature: [`features/items/items.md`](../features/items/items.md) |
| **Phase 3 slice prog-1 — Progression substrate** | New `Progression` module: `ProgressionComponent` (`[Persistent]`, per-track cumulative XP + improvement counts, keyed by `ScoreId`); `IProgressionSystem` (use-driven `AwardExperience`/`TryImprove`/`AwardCombatExperience`, anti-grind scale from raw attributes); `ProgressionEffectContributor` — a third `IEffectContributor` registrant folded by `IStatSystem.Get` with zero interface change (INV-24); `ExperienceAwardHandler` (third independent `MobDiedEvent` subscriber alongside `CurrencyLootHandler`/`SpawnSystem`); `ExperienceAwardedEvent`/`TrackImprovedEvent`; `progress` player command. First slice of the Progression & Balance program; 933 tests total. | [`completed/progression-substrate.md`](completed/progression-substrate.md) · feature: [`features/progression/progression.md`](../features/progression/progression.md) |
| **Phase 3 slice prog-2 — Ascension / character tier** | New `Ascension` module: `AscensionComponent` (`[Persistent]`, player-only, `Tier` scalar 0–6 + `GrantedUnlocks`); `IAscensionSystem` (`GetTier`/`CanAscend`/`TryAscend`/`GetGrantedUnlocks`, reads only raw component state — never `IStatSystem`, confirming the DI-cycle guard a second time); `AscensionEffectContributor` — a fourth `IEffectContributor` registrant folding an additive `TierBaselineStep × tier` baseline with zero interface change (INV-24), layered on top of progression power with no XP reset; admin `ascend [characterName]` command (`CanAscend`→`TryAscend`→one boundary save (INV-22)→`AscendedEvent`+`PlayerAscendedByAdminEvent`); `AscensionNarrationHandler` + extended `AdminAuditHandler`; mobs-only `MobDataComponent`/`MobTemplate.TierBand` content tag (`setmob band`, YAML, Blazor `MobEditor`) — threat emergent from the baseline, no separate multiplier; unlock-grant execution + the player-facing Ascension-Objective gate deferred (empty unlock table ships now). Fixed a stale INV-15 doc note (`IdentityComponent.Tier` never existed). Second slice of the Progression & Balance program; 963 tests total. | [`completed/ascension.md`](completed/ascension.md) · feature: [`features/progression/progression.md`](../features/progression/progression.md) · [`features/progression/ascension-system.md`](../features/progression/ascension-system.md) |
| **Phase 3 slice prog-3 — Power model + balance inspector** | Core-tier, generic `IPowerBudgetSystem`/`PowerBudgetSystem` (`Core/Systems/`, zero constructor dependencies): weighted-sum `Estimate(snapshot, tier)` + derived-band `Classify`/`BandAnchor`, serving three live consumers (INV-19) — admin-gated `power`/`powerband` inspectors, a computed power/band readout on the Blazor `ItemEditor`/`MobEditor`, and the `ProgressionSystem` anti-grind proxy rewired off its inline attribute sum. Authored item tier-band tag (`ItemDataComponent.TierBand`, `setitem band`, YAML round-trip) mirrors the mob band tag prog-2 shipped. Code review caught the plan's own design reaching into the domain `Ascension` module (`AscensionConstants`) from the core-tier oracle (INV-2); fixed by mirroring those constants onto `PowerBudgetConstants` and broadening the architecture guard to catch any `Core/Modules/<Feature>/` import generically. Third slice of the Progression & Balance program; 1008 tests total. | [`completed/power-budget-inspector.md`](completed/power-budget-inspector.md) · feature: [`features/progression/progression.md`](../features/progression/progression.md) |
| **Phase 3 slice prog-3b — Power model revision (Tier × Band)** | Revises `prog-3`'s one-axis Band model into a two-axis Tier (0–6, mechanical) × Band (1–3, purely descriptive) classification: `Classify` returns `PowerBand(Tier, Band)`; new inverse `TargetRange(tier, band)`; recalibrated `PowerBudgetConstants` (`Weights`/`BandSpan`/`BandsPerTier`) for real headroom, mirror constants left locked; `TierBand` splits into a `Tier`+`Band` pair on both item and mob content (clean break, legacy `band:` in `[1,3]` reinterpreted, `[4,6]` untagged) with full authoring parity (`setitem`/`setmob` `tier`/`band` branches); new shared item/mob power-projection seam (`IItemPowerProjectionSystem`/`IMobPowerProjectionSystem`) replacing three hand-rolled inline snapshot builds; new `IBalanceAuditSystem` bulk band-drift sweep feeding a Blazor Integrity report (soft, advisory, never a build/CI gate); new `docs/design/power-model.md` naming the oracle's snapshot-only extensibility principle, folded into the `add-domain-system`/`add-core-system`/`architecture-advisor` tooling. `Estimate`'s algorithm, the three-consumer framework, and the anti-grind rewire hold unchanged. Fourth slice of the Progression & Balance program; 1045 tests total. | [`completed/power-model-revision.md`](completed/power-model-revision.md) · feature: [`features/progression/power-budget-system.md`](../features/progression/power-budget-system.md) |
| **Phase 3 slice sim-1 — Balance standards registry** | Promotes the balance criteria from compiled constants to a designer-editable, YAML-authored balance-standards registry (Spine F): `PowerBudgetConstants`/`BalanceAuditConstants` retired in favor of a single constructor-injected `PowerBudgetTunables` plain-data record (`PowerBudgetSystem`'s sole dependency, still zero domain imports — INV-2), composed by `BalanceInspectionModule` from the new `IBalanceStandardsRegistry` (dense-filled (Tier, Band) table + tunables + band-drift tolerance + outcome tolerances) backed by `IBalanceStandardsStore` (YAML load/validate/save, compiled defaults fallback, fail-fast structural validation, load-time mirror-drift warnings against `AscensionConstants`/`CharacterDefaultsOptions`, atomic tmp→rename write); eagerly resolved at boot via `RegistryValidationBootstrap` on both hosts. `IBalanceAuditSystem`, `powerband`, and both editors' band-mismatch flags now read tolerance/index math from the registry. New Blazor **Standards page** (`/standards`) authors tunables/reference builds/tolerances with a live derived-range preview (throwaway oracle instance) and refuse-vs-warn save (restart-to-apply). First sub-slice of the expanded `prog-4` balance-simulator program; 1064 tests total. | [`completed/balance-standards-registry.md`](completed/balance-standards-registry.md) · feature: [`features/progression/power-budget-system.md`](../features/progression/power-budget-system.md) |
| **Phase 3 slice sim-2 — Simulation engine core** | New `Core/Modules/Simulation/`: data-keyed `ScenarioDefinition` (kind/name/seed/iterations/maxTicksPerRun/sides), three combatant sources (mob template, sim-1 reference build, inline stat block), a hand-built `ISandboxWorldFactory` composing a fresh `EntityService` + full combat/stats/effects/aspects/abilities/entity-state/regeneration/progression/ascension graph per run (never the live world — INV-12 nuance, guard-tested), three built-in `ISimCombatantPolicy` implementations (stateless — cycle position derives from a passed `roundIndex`, not instance state), a `CombatScenarioExecutor` synthetic-heartbeat driver (no event bus), a parallel `ISimulationRunner` with deterministic per-run seeding (`SimSeeds.DeriveRunSeed`, SplitMix64-style, never `HashCode.Combine`) and index-ordered reduce, `ISimOutcomeEvaluator` reading the sim-1 registry's expected-outcome tolerances, and `ISimReportWriter` (atomic JSON, schema v1). New headless `simulate` run-mode (`Server/SimulateRunMode.cs`, mirrors `generate`). The first real simulated fight surfaced a genuine calibration gap — the Ascension tier baseline currently has zero measurable effect on combat outcomes (tracked in `backlog.md`, pinned rather than silently fixed — a balance-design decision outside a plumbing slice's scope). Second sub-slice of the `prog-4` balance-simulator program; 1128 tests total. | [`completed/simulation-engine-core.md`](completed/simulation-engine-core.md) · feature: [`features/simulation/simulation.md`](../features/simulation/simulation.md) |
| **Phase 3 slice sim-3 — Blazor editor integration** | New Blazor **Simulation page** (`/simulation` in `Hedron.Web`) composing/saving/loading any structurally valid `ScenarioDefinition` and launching it as a **background run** via a new host-singleton `SimulationRunService` (single-flight FIFO drain loop, ~750ms status polling survives circuit navigation, cooperative cancellation writes no report). Four small additive engine seams, all backward-compatible (default parameters/new interfaces): `ISimulationRunner.Run` gained an optional `CancellationToken` + `onRunCompleted` progress callback; new `ISimReportReader`/`SimReportReader` sharing the writer's extracted `SimReportJson` options; `ISimScenarioStore` gained `SaveAsync`/`List` (`Simulation:ScenarioDirectory`, atomic tmp→rename, upsert-by-name); `SimCombatantFactory` gained a verdict-cell fallback (an unbanded mob template resolves the scenario spec's `Tier`/`Band` annotation as its cell, authored tag still wins when banded). "Simulate vs reference" buttons on `MobEditor`/`ItemEditor` and a "Re-run baseline sweep" button on the Standards page compose scenarios via two new pure web-side composers (`SimulationPrefill`, `BaselineSweep`) and enqueue through the same run service — no verdict/validation logic duplicated in `Hedron.Web`. `Hedron.Tests` gained a `ProjectReference` to `Hedron.Web` for the new `Hedron.Tests/Web/` tier. Third sub-slice of the `prog-4` balance-simulator program; 1159 tests total. | [`completed/simulation-editor-integration.md`](completed/simulation-editor-integration.md) · feature: [`features/simulation/simulation.md`](../features/simulation/simulation.md) |
