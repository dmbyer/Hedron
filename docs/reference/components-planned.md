# Components Reference — Planned / Idealized

> **Design intent, not a catalog of what exists.** The target component model below is written against archetypes that are themselves not yet built (see [`archetypes.md`](archetypes.md)) and predates the current component shapes. **Do not assume these types exist.** For components that actually exist, see [`components.md`](components.md). Re-audit against reality as each gameplay slice lands (tracked in [`../roadmap/backlog.md`](../roadmap/backlog.md)).

---

## Target cross-cutting components

The intended full component set for living entities, items, and locations once the gameplay slices land. All live under `Core/ECS/Components/` in the target design.

| Component | Shape | Owned by archetypes | Persisted? |
|---|---|---|---|
| `IdentityComponent` | `Name`, `ShortDescription`, `LongDescription`, `Tier`, `WasAutoGenerated` | every archetype | yes |
| `TransformComponent` | parent/child, `RoomId`, `AreaId` | every archetype except Custom | yes |
| `PersistentEffectsComponent` | long-term effects that survive restart (curses, quest debuffs) | Player, Mob | yes |
| `TransientEffectsComponent` | session-only effects (buffs, potion durations, combat buffs) | Player, Mob | no |
| `AttributesComponent` | `Might`, `Finesse`, `Will`, … with base values; modifiers come from effects at read time | Player, Mob | yes |
| `PoolsComponent` | HP / Stamina / Energy (current + base-max; effective max computed via effects) | Player, Mob | yes |
| `CurrencyComponent` | copper, silver, gold, vita, menta, astra | Player, Mob | yes |
| `SkillsComponent` | learned skills, levels, improvement tracking | Player, Mob | yes |
| `QualitiesComponent` | misc qualities / tags | Player, Mob | yes |
| `InventoryComponent` | unequipped carried items, capacity | Player, Mob, Room (ground items) | yes |
| `EquipmentComponent` | items worn in slots (`Head`, `Torso`, `OneHandedWeapon`, …) | Player, Mob | yes |
| `PlayerDataComponent` | privilege level, account-linked state (connection ref is transient) | Player only | yes |
| `PlayerConfigurationComponent` | prompt template, preferences | Player only | yes |
| `MobDataComponent` | behavior flags, level, level effects | Mob only | yes |
| `ItemDataComponent` | `Slot`, `Rarity`, `Material`, `Value`, `Behavior` | every Item archetype | yes |
| `WeaponDataComponent` | `DamageType`, `MinDamage`, `MaxDamage`, `WeaponType` | Weapon | yes |
| `PotionDataComponent` | `PoolRestoration`, `EffectsOnUse` | Potion | yes |
| `ContainerDataComponent` | capacity, contained entity list, access permissions | Room, Area, Storage | yes |
| `RoomDataComponent` | `Exits`, `IsShop`, `Environment`, `IsSafe`, `LightLevel` | Room | yes |
| `AreaDataComponent` | `RespawnRate`, `SpawnableMobs`, `CuratedItems`, `Weather`, `PvP` | Area | yes |

> **Effects components — stub status.** `PersistentEffectsComponent` / `TransientEffectsComponent` are part of the two-level persistence *design* ([../architecture/06-persistence.md](../architecture/06-persistence.md) uses `TransientEffectsComponent` as the canonical "untagged, session-only" example) but no slice has populated them with effect content yet. They graduate to [`components.md`](components.md) when a gameplay slice (combat, potions) gives them real data.

---

## Module-owned candidates

Components anticipated by the roadmap, owned by their feature module once built (`Core/Modules/<Feature>/Components/`).

| Module | Component | Persisted? |
|---|---|---|
| Combat | `CombatStateComponent` | no |
| Combat | `ThreatTableComponent` | no |
| Combat | `CooldownsComponent` | no |
| AI | `BehaviorTreeComponent` | no |
| AI | `FactionComponent` | yes |
| AI | `DialogueComponent` | yes |
| Spawn | `SpawnPointComponent` | yes |
| World | `RoomFlagsComponent` | yes |
